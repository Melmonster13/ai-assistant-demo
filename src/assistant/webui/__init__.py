# Web UI: backend + plain HTML/JS client (no build step, no CDN deps). One of
# the equal-peer interfaces — it terminates at the same Orchestrator entry point
# the CLI uses and voice (Phase 5) will use. Confirmation stays at the
# orchestrator level; this package only supplies the surface that answers it.
