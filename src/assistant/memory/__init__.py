# Memory layer: fact-recall (Postgres+pgvector, user_id-keyed with RLS) and
# persona/profile (hand-authored markdown). A direct integration, not MCP —
# internal, non-third-party, no distinct trust boundary. Isolated from the
# orchestrator's tool-routing internals.
